## 1. Métricas que el ratchet necesita (repo duplexalmar)

- [x] 1.1 Agregar `@vitest/coverage-v8` a las dependencias de desarrollo y configurar en `vitest.config.ts` la cobertura con `provider: 'v8'` y reporters `['text', 'json-summary']`, más el reporter `junit` con salida `.ci/junit.xml`, y verificar con `npm test -- --coverage --reporter=junit --outputFile=.ci/junit.xml` que quedan escritos `coverage/coverage-summary.json` con porcentajes de líneas, sentencias, funciones y ramas y `.ci/junit.xml` con un `<testcase>` por test
- [x] 1.2 Agregar `/coverage/` y `/.ci/` a `.gitignore` y verificar con `git status --porcelain` que tras correr 1.1 no aparece ningún archivo generado sin versionar
- [x] 1.3 Verificar que `npm run check` sigue en verde y que la cobertura reportada es la de la suite actual (no 0 ni 100 por un `exclude` mal puesto), leyendo el resumen de texto

## 2. Consumo del componente para la verificación continua (repo duplexalmar)

- [x] 2.1 Copiar `templates/node/simple/ci.yml` de `ci-workflows` a `.github/workflows/ci.yml` y dejarlo con `on: push` en `main` y `feature/*`, `on: pull_request`, los permisos que el componente exige (`contents: write`, `packages: write`, `pull-requests: write`, `actions: read`) y los inputs `language: node`, `branch_model: simple`, `main_branch: main`, `work_branch_pattern: 'feature/*'`, `image_name: ghcr.io/zaphold2k/duplexalmar`; verificar con `actionlint` (binario en el scratchpad) que no hay errores de sintaxis ni de expresiones
- [x] 2.2 Agregar a `ci.yml` los inputs propios de este repo: `test_command: npm test -- --coverage --reporter=junit --outputFile=.ci/junit.xml`, `e2e_command: npx playwright install --with-deps chromium && npm run test:e2e:docker`, y verificar con `actionlint` y contra `docs/INPUTS.md` que cada input existe con ese nombre y que ninguno de los que quedan en default (`platforms`, `publish_image`, `registry`, `dockerfile`, `coverage_tolerance`) necesita declararse
- [x] 2.3 Agregar a `ci.yml` el smoke test (`smoke_test_endpoint: http://localhost:4321/`, `smoke_test_port: '4321'`, `smoke_test_env` con valores de prueba para `ADMIN_USERNAME`, `ADMIN_PASSWORD_HASH`, `SESSION_SECRET`, `WHATSAPP_NUMBER` y `DATA_DIR`), y verificar en local que `docker run` con esas mismas variables levanta la imagen y `curl -f http://localhost:4321/` responde 200
- [ ] 2.4 Correr el pipeline con `dry_run: true` empujando la rama de trabajo y verificar en el resumen del job que las etapas corren, que las métricas quedan informadas (sin bloquear, por no haber línea de base) y que la publicación que _habría_ hecho es un tag `v0.1.0-alpha.sitio-inicial.1` con su imagen y la etiqueta móvil `sitio-inicial`, sin que nada se haya publicado
- [ ] 2.5 Verificar con un commit temporal en la rama que rompa un test unitario (revertido después) que el flujo falla, que el resumen nombra la etapa `test` y que no se publica ningún tag ni imagen

## 3. Release con release-please (repo duplexalmar)

- [x] 3.1 Copiar `templates/node/release.yml` a `.github/workflows/release.yml` con `target_branch: main` y el secreto `release_token`, y copiar `release-please-config.json` de `templates/node/` a la raíz; verificar con `actionlint` y leyendo el config que las secciones del changelog son las de español y que `bump-minor-pre-major` está activo
- [ ] 3.2 Crear `.release-please-manifest.json` con `{ ".": "0.0.1" }` (la versión que hoy declara `package.json`) y verificar que la versión que release-please propone en su primer PR es `0.1.0`, no `1.0.0` ni `0.0.2`
- [ ] 3.3 Crear el secreto `RELEASE_PLEASE_TOKEN` en el repositorio (PAT con `contents: write`) y verificar, al aceptar el primer PR de release, que el tag creado dispara `ci.yml` y publica la imagen sin intervención
- [ ] 3.4 Verificar que Prettier no pelea con los archivos generados: correr `npm run format:check` con `release-please-config.json`, `.release-please-manifest.json` y el `CHANGELOG.md` del primer PR en el árbol, y agregar a `.prettierignore` los que el formateador quiera reescribir

## 4. Documentación de flujo generada (repo duplexalmar)

- [x] 4.1 Correr `render-docs.mjs` del componente con `--language node --branch-model simple --main-branch main --work-branch-pattern 'feature/*' --image ghcr.io/zaphold2k/duplexalmar --readme README.md --agent-docs CLAUDE.md`, y verificar que el bloque queda instalado entre sus marcadores en `README.md` y que `CLAUDE.md` se crea con el mismo bloque
- [x] 4.2 Verificar la convivencia con Prettier: correr `npm run format` después de generar y comprobar con `render-docs.mjs --check` que el bloque sigue siendo el esperado; si Prettier lo reescribe, agregar los archivos afectados a `.prettierignore` y volver a comprobarlo
- [ ] 4.3 Declarar en `ci.yml` `readme_path: README.md` y `agent_docs: CLAUDE.md`, y verificar con un cambio temporal en el input `work_branch_pattern` (revertido después) que el flujo falla indicando que la documentación quedó desactualizada

## 5. Documentación propia y contrato de ejecución (repo duplexalmar)

- [x] 5.1 Agregar al README la sección "Versiones" en español: que empujar una rama `feature/*` publica una pre-release con su imagen, que la release estable se corta aceptando el PR de release-please, dónde se ve el changelog (`CHANGELOG.md` y `gh release view`), qué hacer si una release queda sin imagen (re-correr `ci.yml` sobre el tag) y que la excepción del ratchet es la etiqueta `ci-ratchet-override` puesta por una persona, nunca por un agente; verificar con `npm run format:check` y leyéndolo como persona nueva que no hace falta abrir ningún workflow para seguirlo
- [x] 5.2 Agregar al README la sección "Ejecutar la imagen en producción" con la tabla del contrato de ejecución (decisión 9 del design: volumen de datos escribible por uid 1000, variables de entorno, puerto interno sin publicar, healthcheck, límite de memoria, proxy con `X-Forwarded-*` delante, lote de subida por debajo del límite del proxy, versión leída de la etiqueta), la aclaración de que el `docker-compose.yml` de este repo es para desarrollo y sigue usando `build: .`, y la indicación de que el procedimiento de despliegue vive en el repo de despliegue; verificar que la tabla no nombra ningún host, inventario ni ruta del entorno
- [ ] 5.3 Verificar que el `Dockerfile` no necesita cambios: inspeccionar una imagen publicada por el pipeline y comprobar que trae `org.opencontainers.image.source`, `.version` y `.revision` con el repositorio, la versión y el commit correctos, puestas por el componente

## 6. Despliegue (se implementa en el repo de despliegue)

> El detalle de inventarios, hosts, rutas y comandos se decide y se documenta allá, siguiendo sus convenciones. Acá sólo queda registrado qué tiene que quedar hecho y cómo se verifica.

- [ ] 6.1 Preparar la máquina de control: `ansible-core` y `ansible-lint` con `pipx`, más las dependencias que declare el repo de despliegue, y verificar que responde un `ping` al entorno y que el playbook corre en modo ensayo sobre un servicio ya existente sin errores
- [ ] 6.2 Confirmar en la preparación que la arquitectura del host es `aarch64`; si no lo fuera, volver a la decisión 4 del design antes de seguir
- [ ] 6.3 Definir el servicio del sitio en el repo de despliegue cumpliendo el contrato de ejecución del README (imagen fijada a `0.1.0`, volumen de datos, `.env` obligatorio, sin puertos publicados, healthcheck, límite de memoria, red del proxy), y verificar con el lint y el ensayo del playbook que la definición es válida, que el servicio generado no publica ningún puerto y que la etiqueta de imagen es una versión concreta y no `latest` ni el nombre de una rama
- [ ] 6.4 Dejar en el repo de despliegue el comando que fija una versión, muestra el diff, ensaya, aplica y commitea, y verificar con una versión inexistente que falla al bajar la imagen, que lo que estaba corriendo sigue igual y que el cambio no queda commiteado
- [ ] 6.5 Dejar en el repo de despliegue el runbook de esta app: preparación única del entorno (volumen de datos con el dueño que espera la imagen, `.env` a partir de `.env.docker.example` con `MAX_UPLOAD_BATCH_SIZE=6`, red del proxy, dirección pública apuntando al puerto interno del contenedor), despliegue, vuelta atrás y qué hacer tras cambiar el `.env`; verificar que se puede seguir de arriba a abajo sin conocimiento previo

## 7. Primera publicación real y verificación de punta a punta

- [ ] 7.1 Quitar `dry_run` de `ci.yml`, empujar la rama y verificar que sale la primera pre-release (`0.1.0-alpha.sitio-inicial.N`) con su imagen, midiendo cuánto tarda la construcción `arm64` emulada; si no cierra, recortar `platforms` a `linux/amd64` para no bloquear el resto y abrir en `ci-workflows` el change de runners ARM nativos (ver el riesgo correspondiente del design), dejando anotado acá el tiempo medido como evidencia
- [ ] 7.2 Hacer público el paquete `duplexalmar` en la configuración de paquetes de GitHub (una sola vez, a mano) y verificar que `docker logout ghcr.io && docker pull ghcr.io/zaphold2k/duplexalmar:0.1.0-alpha.sitio-inicial.1` funciona sin credenciales y que `docker manifest inspect` lista `amd64` y `arm64`
- [ ] 7.3 Preparar el entorno siguiendo el runbook de 6.5 y verificar que el volumen de datos es escribible por el usuario de la imagen, que el archivo de configuración existe con permisos restrictivos y que la red del proxy existe
- [ ] 7.4 Mergear la rama a `main` y verificar que release-please abre el PR de release con `0.1.0` y su changelog agrupado en español; aceptarlo y verificar que quedan el tag `v0.1.0`, el `CHANGELOG.md` en `main`, la release en GitHub y las imágenes `0.1.0`, `0.1` y `latest` con el mismo digest, sin etiqueta `0` pelada
- [ ] 7.5 Desplegar `0.1.0` y verificar que el contenedor queda `healthy` con esa imagen, que el sitio responde por la dirección pública con TLS y que `/images/...` llega con `Cache-Control: public, max-age=31536000, immutable`
- [ ] 7.6 Verificar la subida real detrás del proxy: iniciar sesión en el panel por la dirección pública desde un celular, subir una foto y reordenar la galería, y comprobar que aparecen en el sitio público (esto valida `X-Forwarded-Proto` frente a `checkOrigin` y los binarios `arm64` de sharp)
- [ ] 7.7 Verificar la vuelta atrás: desplegar la pre-release `0.1.0-alpha.sitio-inicial.N`, comprobar que el sitio sigue mostrando las mismas fotos y orden, y volver a `0.1.0`; el historial del repo de despliegue debe registrar los tres despliegues
- [ ] 7.8 Cerrar el lado del componente: en `~/github_ci` (change `add-reusable-ci-workflows`, o por medio de la sesión `github-ci-3e`) marcar 12.4 con la evidencia del ensayo sin efectos de la tarea 2.4 y 12.5 con la del pipeline real de 7.1–7.4, y revisar el resto de las tareas abiertas por si esta adopción ya las verificó —en particular las de construcción multi-plataforma (6.3, 6.7), documentación generada (parte de la sección 9) y ciclo de ramas del modelo simple—; verificar con `openspec status --change add-reusable-ci-workflows --json` que el conteo avanzó y que ninguna tarea quedó marcada sin evidencia real
- [ ] 7.9 Verificar que el ratchet ya bloquea con línea de base: abrir un PR de prueba que saltee un test (descartado después) y comprobar que el flujo falla nombrando la métrica que retrocedió y el valor contra el que comparó
